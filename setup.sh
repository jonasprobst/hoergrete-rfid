# TODO: add the comands needed to setup this project ;-)
# 
# Service Hoergrete
# - copy hoergrete.serive to `/etc/systemd/system`
# - reload the service files to include hoergrete: `sudo systemctl daemon-reload`
# - start service: `sudo systemctl start hoergrete.service`
# - check status: `sudo systemctl status hoergrete.service`
# - if all looks well enabel it on every reboot: `sudo systemctl enable hoergrete.service`
# - Logs: `sudo journalctl -u hoergrete` (-n 10 for the last 10 entries only)

